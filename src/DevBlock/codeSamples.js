const codeSamples = {
  js: [
    "import Four from 'Number'",
    "  const Five = (One) => {",
    "    return Four + One",
    "  }",
    "export default Five",
    "import Four from 'Number'",
    "  const Five = (One) => {",
    "    return Four + One",
    "  }",
    "export default Five",
    "import Four from 'Number'",
    "  const Five = (One) => {",
    "    return Four + One",
    "  }",
    "export default Five",
    "import Four from 'Number'",
    "  const Five = (One) => {",
    "    return Four + One",
    "  }",
    "export default Five"
  ]
}

export default codeSamples

